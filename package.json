import json

# Création du contenu du package.json
package_json = {
    "name": "bot-convocation-cour",
    "version": "1.0.0",
    "description": "Bot Discord pour la gestion des convocations à la cour",
    "main": "index.js",
    "scripts": {
        "start": "node index.js"
    },
    "dependencies": {
        "discord.js": "^14.14.1",
        "dotenv": "^16.4.5"
    },
    "engines": {
        "node": ">=16.9.0"
    },
    "author": "",
    "license": "ISC"
}

# Écriture du fichier dans un format lisible
with open('package.json', 'w', encoding='utf-8') as f:
    json.dump(package_json, f, indent=4)
